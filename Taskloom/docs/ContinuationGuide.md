# Контекст продолжения работы

## Назначение

Этот документ нужен для продолжения работы над проектом после потери текущей сессии. Новый исполнитель должен сначала прочитать этот файл, затем `ProjectOverview.md`, `Architecture.md`, `DevelopmentPlan.md`, `Decisions.md` и `WorkLog.md`.

## Что это за проект

Taskloom - персональное desktop-приложение для Windows, ориентированное на календарь и работу с разными типами записей, привязанными ко времени и датам.

Это не обычный todo list и не простое notes app. Базовая модель проекта должна строиться вокруг календарной записи, а не вокруг одной сущности задачи.

## Технологические ограничения

- C#
- .NET 10
- WPF
- MVVM
- CommunityToolkit.Mvvm
- SQLite
- Dapper

## Внешние зависимости запуска

- Проект собирается под `net10.0-windows10.0.19041.0`.
- Для запуска пользователю нужен установленный .NET 10 Desktop Runtime.
- Для Windows notifications через `AppNotificationManager` нужен установленный Windows App Runtime 1.8.
- Если Windows App Runtime 1.8 отсутствует или установлен неполно, приложение не должно падать: используется fallback через tray balloon.
- Для audio metadata и cover art используется пакет `TagLibSharp`.
- Для video link preview на первом этапе используется provider-specific enrichment без зависимости сохранения записи от сети; первый реализованный provider — `YouTube`.
- Для richer metadata YouTube используется бесплатный `YouTube Data API` через optional переменную окружения `TASKLOOM_YOUTUBE_API_KEY`.

Нельзя без серьёзной причины переводить проект на:

- web stack
- MAUI
- WinUI
- heavy frameworks
- EF Core

## Правила коммуникации и оформления

- Все ответы пользователю писать на русском языке.
- Вся документация в проекте должна быть на русском языке.
- Все комментарии в коде должны быть на русском языке.
- Все XML documentation comments должны быть на русском языке.
- Все идентификаторы кода должны быть на английском языке.
- Имена должны быть чистыми и профессиональными в стиле C#.

## Правила реализации

- Не переопределять базовые решения проекта без серьёзной причины.
- Работать строго пошагово.
- Не создавать лишние абстракции.
- Не переносить бизнес-логику во View.
- Использовать MVVM последовательно.
- Использовать `ObservableObject`, `ObservableProperty`, `RelayCommand` там, где это действительно уместно.
- Не проглатывать исключения.
- Не вводить хаков и временных решений без явной необходимости.
- Перед созданием новых сущностей проверять, нет ли уже подходящих.
- Все пользовательские строки интерфейса должны проходить через локализацию.
- Основные цвета интерфейса должны идти через единый theme layer.
- Главное окно должно сохранять приоритет календаря как основного элемента навигации.

## Правила Git и GitHub

- Рабочая ветка разработки: `dev`.
- `master` не использовать как основную ветку активной разработки.
- Пуш выполняет пользователь вручную.
- `docs` должны входить в коммиты, если изменился код, архитектура, план, процесс или правила работы.
- Корневой `README.md` нужно рассматривать как часть поддерживаемой документации репозитория и обновлять при значимых изменениях возможностей, зависимостей, сборки или позиционирования проекта.
- Перед коммитом нужно проверять staged-файлы, а не коммитить всё вслепую.
- В коммит не должны попадать `.idea`, `bin`, `obj`, локальные журналы и временные SQLite-артефакты.

## Правило поведения для помощника при запросе "добавить к коммиту"

- Если пользователь просит добавить изменения к коммиту, нужно сначала проверить `git status`.
- Затем нужно добавить в staging только релевантные файлы для текущего шага.
- `docs` нужно включать в staging, если они должны отражать текущее изменение.
- Нельзя автоматически добавлять IDE-мусор и нерелевантные пользовательские файлы.
- Если в рабочем дереве есть сомнительные или несвязанные изменения, нужно прямо указать это пользователю.

## Текущая архитектурная позиция

- Используется один WPF-проект `Taskloom`.
- Разделение выполнено по папкам: `Assets`, `Common`, `Data`, `Domain`, `Infrastructure`, `Presentation`, `Services`, `docs`.
- В `Domain` уже реализована базовая доменная модель.
- Документация находится внутри `Taskloom/docs`, чтобы быть видимой в Rider.

## Что уже сделано

- Создан стартовый каркас WPF-проекта.
- Подготовлена структура каталогов.
- Создана и ведётся обязательная документация.
- Реализована доменная модель:
  - `CalendarRecord`
  - `TaskRecord`
  - `NoteRecord`
  - `EventRecord`
  - `DaySummaryRecord`
  - `RecordType`
- Спроектирована storage-модель и базовая SQLite schema.
- Реализованы инфраструктура SQLite и базовый репозиторий календарных записей.
- Реализован прикладной сервис записей и draft-модель для редактора.
- Реализованы базовые ViewModels и стартовая композиция приложения в `App`.
- Реализованы полноценные WPF Views главного окна и редактора записи.
- Выполнен базовый рефайнмент MVP-сценариев `create/edit/delete`.
- Согласован следующий этап: локализация интерфейса, настройки приложения и увеличение календаря.
- Реализованы локализация интерфейса, отдельные языковые файлы, окно настроек и увеличенный календарь.
- Выполнен UX-рефайнмент списка записей: адаптивные карточки, мягкое выделение, лучшее использование ширины окна.
- Выполнено продолжение UX-рефайнмента главного окна: переработаны хедер, блок статуса, кнопки действий и контейнер календаря.
- Дополнительно усилен приоритет календаря по размеру и исправлено чрезмерное растягивание одиночных карточек записей.
- Согласован следующий этап: `calendar-first` перекомпоновка главного окна, явное выделение выбранной записи и поддержка цветовых схем.
- Этап `calendar-first layout и themes` реализован: компоновка главного окна переработана, selected-state усилен, темы и их переключение добавлены в настройки.
- Следующий этап согласован: quick toggle статуса задач через status chips в списке.
- Этап quick toggle статуса задач реализован: статус меняется прямо из карточки, а цвет и состояние чипа зависят от активной темы.
- Следующий подэтап согласован: visual refinement chips и semantic palettes.
- Этап visual refinement chips и semantic palettes реализован: геометрия chips централизована, палитры тем согласованы, лишние акценты карточек ослаблены.
- Главное окно использует кастомную title bar через `WindowChrome`; code-behind содержит только UI-оркестрацию оконных действий.
- Левая календарная панель приведена к структуре `статус / центральный блок Calendar + navigation buttons`, без видимых внутренних контейнеров.
- Белая подложка стандартного `Calendar` гасится локальными `CalendarPanelStyle` и `CalendarItem` template в `MainWindow.xaml`; простые brush-ресурсы и `SystemColors.*` не убирали системный белый фон полностью.
- Редактор события больше не использует ручной ввод строк времени и 15-минутные интервалы: время выбирается отдельными селекторами часов, минут и секунд через `TimePartOptionViewModel`.
- `TimePartOptionViewModel` должен переопределять `ToString()` и возвращать `Title`, потому что закрытое состояние текущего `ComboBox` template может получить сам option-объект.
- Карточка события показывает `LocationDisplay`, если у `EventRecord` заполнено место.
- `EventRecord` теперь хранит `Status` и `ReminderMinutesBefore`; эти поля проходят через draft, data model, SQLite и редактор.
- Напоминания событий реализованы через планировщик `WindowsBalloonEventReminderService`, а транспорт по-прежнему вынесен в `IAppNotificationService`.
- Основной транспорт уведомлений теперь реализован через Windows App SDK `AppNotificationManager`.
- `WindowsBalloonAppNotificationService` сохранён как fallback, если Windows App SDK notifications недоступны в текущей среде или регистрация не удалась.
- Для Windows App SDK проект переведён на `net10.0-windows10.0.19041.0`, добавлены `WindowsPackageType=None` и runtime identifiers.
- Для рабочего notification flow на пользовательской машине требуется установленный пакет Windows App Runtime 1.8, включая `MicrosoftCorporationII.WinAppRuntime.Main.1.8` и `MicrosoftCorporationII.WinAppRuntime.Singleton`.
- Для задач добавлено собственное nullable-время напоминания `TaskReminderTime`; оно хранится отдельно от event-only поля `ReminderMinutesBefore`.
- Для всех типов записей добавлено несколько изображений-вложений через отдельную таблицу `record_attachments`; файл копируется в `%LocalAppData%\Taskloom\Media\Images`.
- Ресурсы записей больше не хранятся плоско в `%LocalAppData%\Taskloom\Media\...`; теперь для каждой записи используется отдельный каталог `%LocalAppData%\Taskloom\Records\<record-id>\...`.
- Вложения изображений имеют `sort_order`; в редакторе их можно добавлять, удалять и переставлять, а карточка записи на главном экране показывает grid миниатюр и открывает конкретное изображение системным просмотрщиком по клику.
- Для всех типов записей добавлены audio attachments; редактор умеет импортировать несколько `mp3/wav/m4a/flac/wma/ogg`, а карточка записи показывает playlist с cover, play/pause, seek и открытием файла через shell.
- Для YouTube video links под описанием записи показываются compact preview cards; thumbnail кэшируется в `%LocalAppData%\Taskloom\Records\<record-id>\link-previews`, а metadata хранится в `link-previews.json`.
- При наличии `TASKLOOM_YOUTUBE_API_KEY` карточка YouTube дополнительно получает `description` и `duration` из официального бесплатного API; без ключа UI просто скрывает эти поля.
- Даже без `TASKLOOM_YOUTUBE_API_KEY` YouTube provider теперь пытается взять `title` и `description` из публичного HTML страницы через `og:title` / `title` / `og:description`, прежде чем уйти в generic fallback.
- Общий pipeline видео-ссылок уже умеет классифицировать `YouTube`, `Twitch`, `VK Видео` и `RuTube`, но отдельный enrichment-provider пока реализован только для `YouTube`.
- Для каждой записи рядом с ресурсами поддерживается metadata-файл `record.json`; его обновляет `RecordResourceMetadataService`.
- Полноценный `strip mode` с drag-scroll ещё не реализован; текущий завершённый этап — multiple images + grid layout.
- Описание записи в карточке умеет распознавать ссылки без изменения исходного текста: поддерживаются URI со схемой, домены без схемы, email и IPv4; открытие идёт через системный обработчик `UseShellExecute=true`.
- Tray mode реализован через `WindowsTrayService`: двойной клик по tray icon открывает окно, меню содержит `Открыть`, `Настройки`, `Выход`.
- Крестик главного окна теперь скрывает приложение в трей; реальное завершение идёт через `Выход`.
- Временная иконка приложения находится в `Assets/Icons/Taskloom.ico`; её можно заменить позже без изменения архитектуры.
- `AppSettings` хранит параметры главного окна: признак сохранённого placement, ширину, высоту и состояние окна.
- `MainWindow` не сохраняет координаты окна; сохранённый размер центрируется при старте, чтобы не зависеть от прошлой конфигурации мониторов.
- Закрытие `MainWindow` сохраняет placement асинхронно. Нельзя возвращать синхронное ожидание async-настроек в UI-потоке, это уже приводило к зависанию приложения при закрытии.
- В `ThemeService` подключены тринадцать тем: `WarmLight`, `NeutralLight`, `GrayLight`, `SoftDark`, `DeepDark`, `Windows11`, `Ubuntu`, `Windows7`, `MacOs`, `FrogGreen`, `VolcanicFire`, `CosmicCold`, `SnowWhite`.
- Все новые темы должны иметь полный набор `App.*Brush` ресурсов, включая status info/warning/success/danger и chip brushes.
- Стилизационные темы под ОС должны оставаться theme resources, а не отдельными шаблонами контролов или попыткой копировать системный UI.
- `ComboBox` и `ComboBoxItem` стилизованы глобально в `App.xaml`; если появятся новые popup/dropdown controls, их нужно отдельно проверить в тёмных темах.
- `DatePickerTextBox` имеет глобальный template в `App.xaml`; белую рамку стандартного шаблона нельзя возвращать.
- Popup-календарь `DatePicker` шаблонизирован в `App.xaml` через `ThemedPopupCalendarStyle`, `CalendarStyle` и `PART_Calendar`; без этих частей WPF может снова показать стандартный белый календарь.
- В `ThemedPopupCalendarStyle` `PART_CalendarItem` обязательно должен получать `Style="{TemplateBinding CalendarItemStyle}"`; иначе WPF вернёт стандартную белую рамку `CalendarItem`.
- В calendar button templates нельзя возвращать `ContentPresenter` для чисел. В главном календаре используем явный `TextBlock` с `Foreground="{DynamicResource App.TextPrimaryBrush}"`, потому что `TemplateBinding Foreground` не перебил системное состояние.
- Для главного `Calendar` стили кнопок дней должны подключаться через свойства `CalendarButtonStyle` и `CalendarDayButtonStyle`; implicit styles внутри `Calendar.Resources` не применялись к сгенерированным дням.
- В popup-календаре `DatePicker` внешний border намеренно прозрачный; видимую рамку должен задавать сам theme-aware calendar, а не стандартный белый popup.
- В тёмных темах primary/info акценты переведены в графитово-серую гамму, чтобы не было инородного сине-голубого цвета.
- В `MainWindow.xaml` календарь содержит локальные templates для `Calendar`, `CalendarItem`, `CalendarButton`, `CalendarDayButton`; это сделано ради читаемости и устранения системных светлых подложек.
- `SettingsWindow` и `RecordEditorWindow` тоже используют custom title bar через `WindowStyle=None` и `WindowChrome`; стандартную системную шапку для новых вспомогательных окон не возвращать без отдельного решения.
- Вспомогательные окна не должны сливаться с фоном: использовать внешнюю рамку `App.BorderStrongBrush`, внутренний фон `App.SurfacePrimaryBrush` и шапку `App.SurfaceSecondaryBrush`.
- `MainWindow` и `RecordEditorWindow` подключают `WindowMaximizeBoundsHelper`; без него безрамочное maximized-окно может уходить под панель задач.
- В настройках кнопка `Применить` только сохраняет язык и тему, но не закрывает окно. Закрытие выполняется крестиком в кастомной title bar.
- Окно настроек переведено на instant apply: язык, тема и политика очистки применяются сразу при выборе, отдельной кнопки `Применить` больше нет.
- Для опасных режимов очистки всех старых записей окно настроек должно запрашивать подтверждение и откатывать выбор при отказе пользователя.
- В редакторе записи блоки параметров конкретного типа должны скрываться целиком на уровне контейнера, а не только отдельных полей внутри блока.
- В длинных формах действия `Сохранить` и `Отмена` должны быть закреплены вне `ScrollViewer`; прокручиваться должна форма, а не критичные кнопки.
- `AppSettings` теперь также хранит `RecordCleanupMode`.
- На старте приложение выполняет auto-cleanup старых записей по выбранной политике; безопасные режимы затрагивают только завершённые задачи и прошедшие события, опасные — вообще все записи старше порога.
- Сбой startup cleanup не должен останавливать запуск: ошибка только логируется в `%LocalAppData%\Taskloom\record-cleanup.log`.

## На чём работа остановилась

Доменная модель, data model, SQLite schema, persistence-слой, прикладной слой, ViewModels, базовые Views, рефайнмент MVP и базовая локализация завершены.

После локализации были обнаружены и исправлены два критических дефекта:

1. Падение при открытии окна настроек.
2. Падение при отображении созданной задачи из-за некорректной WPF-привязки к read-only свойству `IsCompleted`.

Следующий шаг по плану:

1. Проверить настройки: язык, тема и политика очистки должны применяться сразу при выборе, без кнопки `Применить`.
2. Проверить быстрые последовательные переключения темы и языка в окне настроек; мигания тем, задержки закрытия и падения приложения быть не должно.
3. Проверить startup cleanup: режим `Never`, оба опасных режима и оба безопасных режима; убедиться, что удаляются и SQLite-записи, и каталоги `%LocalAppData%\Taskloom\Records\<record-id>`.
4. Проверить, что при сохранении записи создаётся или обновляется `%LocalAppData%\Taskloom\Records\<record-id>\record.json`.
5. Проверить, что сбой удаления файловой папки или metadata не валит startup; детали должны уйти в `%LocalAppData%\Taskloom\record-cleanup.log`.
6. Проверить визуально календарную панель, отсутствие белой подложки у стандартного `Calendar`, отображение места события и цветного статуса события в карточке.
7. Проверить вручную напоминание: создать событие на ближайшее время со статусом `Запланировано` и напоминанием `В момент события` или `За 5 минут`.
8. Проверить запуск главного окна: первый старт должен быть `Maximized`, последующие старты должны брать сохранённый размер и центрировать окно.
9. Проверить переключение всех тринадцати тем в окне настроек.
10. Проверить вспомогательные окна: системной шапки быть не должно, перетаскивание за кастомную верхнюю область должно работать, границы окна должны читаться во всех темах.
11. Проверить maximized-состояние главного окна и редактора: нижняя граница должна доходить до панели задач, но не уходить под неё.
12. Проверить редактор записи: `Параметры задачи` видны только для задачи, `Параметры события` видны только для события.
13. Проверить редактор записи на обычном размере экрана: окно открывается нормальной высотой, кнопки снизу видны без прокрутки.
14. Проверить селекторы времени события: начало и конец выбираются как `HH:MM:SS`, а в закрытом поле не отображается сырое значение `TimeOnly`.
15. Проверить уведомления события: отдельно `Скоро событие` в момент напоминания и `Событие началось` в момент старта.
16. Проверить Windows App SDK notifications: уведомление должно появляться как системное и оставаться в центре уведомлений Windows 11.
17. Проверить tray mode: крестик скрывает окно, двойной клик по tray icon открывает, `Настройки` открывает окно настроек, `Выход` завершает приложение.
18. Проверить task reminders: pending-задача с включённым временем должна прислать одно уведомление на своей дате, completed-задача не должна уведомлять.
19. Проверить multiple image attachments: `jpg/png/bmp/gif` должны импортироваться пачкой, порядок после стрелок должен сохраняться, удаление одной картинки не должно затрагивать остальные.
20. Проверить grid gallery в карточке: несколько миниатюр должны открывать именно выбранный файл, а при количестве больше четырёх должен отображаться хвост `+N`.
21. Проверить audio playlist: импорт нескольких файлов, title fallback на имя файла, cover art, play/pause, seek и переключение между треками в разных карточках.
22. Следующим отдельным этапом остаётся `strip mode` с горизонтальным drag-scroll внутри контейнера.
23. Проверить link parsing в описании: `http/https/ftp/tg/mailto/file`, `youtube.com`, `tanki.su`, `192.168.34.165`, `user@example.com`, пунктуация после ссылки и несколько ссылок в одной строке.
24. Проверить themed-окна подтверждения: удаление записи и dangerous cleanup confirmation должны выглядеть как часть приложения и наследовать текущую тему.
25. Проверить первый запуск после сборки: первое нажатие на `Новая запись` должно сразу открывать редактор.
26. Проверить created-time badge: у всех новых и существующих записей он должен отображаться компактно и не конфликтовать с другими chips.
27. Проверить audio metadata line: album/genre должны появляться только при наличии тегов и не ломать высоту карточки без метаданных.
28. Проверить tooltip-подсказки на обрезаемых строках карточки: заголовок, место события, описание и аудио-метаданные должны показывать полный текст без расширения карточки.
29. Проверить выделение записи: синяя полоса слева должна прилегать к скруглённому краю без визуальных зазоров.
30. Проверить снятие выделения кликом по пустым областям окна; кнопка `Редактировать` после этого должна становиться неактивной.
31. Проверить video link previews для YouTube: title, thumbnail, opening in system browser, fallback на placeholder при сетевой ошибке и повторную попытку на следующей загрузке карточки.
32. Следующий шаг в этой подсистеме: отдельный бесплатный Twitch provider через `TASKLOOM_TWITCH_CLIENT_ID` и `TASKLOOM_TWITCH_CLIENT_SECRET`, затем providers для VK Видео и RuTube.
33. После этого зафиксировать текущий срез отдельным коммитом или выбрать следующий функциональный этап.
34. При дальнейшей работе не возвращаться к локальным жёстким цветам мимо theme layer.
35. Все новые пользовательские строки добавлять только через локализацию.
36. Все основные цвета переводить на theme resources.

## Что нужно сделать следующим исполнителем

Сначала:

1. Прочитать этот файл.
2. Проверить `DevelopmentPlan.md`.
3. Проверить `WorkLog.md`.
4. Изучить содержимое папки `Domain`.
5. Изучить содержимое папки `Data`.
6. Изучить содержимое папки `Infrastructure`.
7. Изучить содержимое папки `Services`.
8. Изучить содержимое папки `Presentation`.

Потом:

1. Проверить `git status --short` и staged-состав для текущего шага.
2. Проверить приложение вручную: startup cleanup, instant-apply settings, Windows notifications в центре уведомлений, task reminders, multiple image attachments, grid gallery в карточках, tray mode, темы, редактор события, уведомления события.
3. После значимого шага обновить `docs` и `README.md`, если затронуты пользовательские возможности, зависимости или сборка.
4. Следующий функциональный этап выбрать после ручной проверки очистки, аудио, напоминаний и вложений; ближайшие кандидаты — `strip mode` для изображений и отдельный gallery/viewer для 10+ изображений.
5. Не возвращаться к hardcoded UI-строкам в новых экранах и окнах.
6. Не вводить новые локальные цвета в XAML мимо theme layer.
7. Для коммитов использовать короткий commit message на английском, прямо описывающий итоговый срез.

## Текущий staged-срез перед прерыванием сессии

Перед завершением сессии в staging был добавлен большой срез примерно из 55 файлов:

- переработка `App.xaml` и theme-aware templates для базовых контролов;
- новые темы и расширение `ThemeService`;
- статусы событий, напоминания событий и отображение места события;
- точный выбор времени события через `HH:MM:SS`;
- custom title bar для основного и вспомогательных окон;
- исправление maximized-границ безрамочных окон;
- tray mode и временная иконка `Assets/Icons/Taskloom.ico`;
- обновление SQLite schema/data model/repository для событий;
- обновление всей проектной документации.

Рекомендуемый commit message:

```text
Расширить UI, темы, события и фоновый режим приложения
```

Рекомендуемый commit body:

```text
- Добавлены статусы событий, напоминания и отображение места события.
- Переработан редактор записи: точный выбор времени HH:MM:SS, custom title bar, закреплённые кнопки.
- Добавлены theme resources, тёмные, ОС-стилизованные и авторские цветовые темы.
- Улучшены календарь, карточки записей, chips, контраст вспомогательных окон и maximized-поведение.
- Добавлены временная иконка приложения, tray mode и базовый транспорт уведомлений.
- Обновлены SQLite schema, data/domain/service слои и проектная документация.
```

## Предпочтительное направление для data model

Нужно сохранить выразительность домена и при этом не усложнить MVP.

Уже выбран такой подход:

- одна таблица записей с общими полями
- поле типа записи
- nullable-поля для специфики типов там, где это оправдано для MVP

Это не означает, что домен должен стать плоским. Плоской может быть storage-модель, но не доменная.

## Обязательное правило на будущее

После каждого значимого шага нужно обновлять:

- `README.md`, если изменились ключевые возможности, зависимости, сборка или публичное описание проекта
- `ProjectOverview.md`
- `DevelopmentPlan.md`
- `WorkLog.md`
- `Architecture.md`
- `Decisions.md`
- этот файл, если изменилась точка продолжения или рабочие правила
